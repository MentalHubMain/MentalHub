Instructions:
    "adding a new game": "add a new block under games, give it any name you want, throw all the place ids in the ids list, paste the script url, and set keyless to false. thats it.",
    "adding a keyless game": "same as above but keyless set to true, the loader will handle the getgenv line automatically so dont worry about that",
    "multiple ids same game": "just dump all the ids in the same ids array, they all point to the same url so no need to copy paste the url multiple times",
    "making changes": "just edit this file and commit, changes go live instantly, no need to touch the loader worker at all"
